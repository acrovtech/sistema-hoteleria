DATABASE_URL="postgresql://postgres:hotel123@localhost:5432/hotel"
# Genera uno nuevo por entorno: openssl rand -hex 32
JWT_SECRET="cambia-esto-en-produccion"
# Orígenes permitidos por CORS, separados por coma (dominio del admin en prod)
CORS_ORIGIN="http://localhost:3000"
ENABLE_PUBLIC_WEBSITE="false"
ENABLE_BOOKING_ENGINE="false"
PLAN="ADMIN_ONLY"
PORT=3001
