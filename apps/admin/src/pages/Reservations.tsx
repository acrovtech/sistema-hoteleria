export default function Reservations() {
  return (
    <div>
      <h1>Reservas / Check-in / Check-out</h1>
      <p>Calendario + creación manual de reserva (origen ADMIN). Próximo: formulario + cambio estado habitación.</p>
    </div>
  );
}
