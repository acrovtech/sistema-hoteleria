packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 11 bloquea postinstall scripts por defecto (ERR_PNPM_IGNORED_BUILDS).
# esbuild (Vite) y bcrypt (API) necesitan los suyos para funcionar.
allowBuilds:
  esbuild: true
  bcrypt: true
